export interface Notification {
  id: number;
  icon: string;
  iconBg: string;
  title: string;
  time: string;
}

export interface Activity {
  id: number;
  avatar: string;
  title: string;
  time: string;
}

export interface Contact {
  id: number;
  avatar: string;
  name: string;
}
